<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.PaceLegends" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Starting Theme (Splash Screen) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#1A1A2E</item>
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.PaceLegends</item>
    </style>
</resources>
